# Время ожидания ответа от сервера (в секундах)
timeout 60;

# Повторные попытки
retry 60;

# Отправка имени хоста серверу
send host-name "<имя_хоста>";

# Запрос конкретного IP-адреса
# send dhcp-requested-address <IP-адрес>;
