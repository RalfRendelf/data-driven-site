#include <pwd.h>
#include <unistd.h>
#include <iostream>

int main() {
    passwd* pw = getpwuid(getuid());
    std::cout << "User: " << pw->pw_name << std::endl;
    std::cout << "Home: " << pw->pw_dir << std::endl;
    std::cout << "Shell: " << pw->pw_shell << std::endl;
}
