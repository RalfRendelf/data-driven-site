#include <signal.h>
#include <iostream>
#include <unistd.h>

void handler(int sig) {
    std::cout << "Received signal: " << sig << std::endl;
}

int main() {
    signal(SIGINT, handler);
    while (true) {
        sleep(1);
    }
}
