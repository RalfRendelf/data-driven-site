#include <unistd.h>
#include <iostream>

int main() {
    pid_t pid = fork();

    if (pid == 0) {
        std::cout << "Child PID: " << getpid() << std::endl;
    } else {
        std::cout << "Parent PID: " << getpid() << ", child: " << pid << std::endl;
    }
}
