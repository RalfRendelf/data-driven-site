[Service]
# Изменяем пользователя, от имени которого запускается сервис
User=myuser
Group=mygroup

# Добавляем переменную окружения
Environment="MY_VAR=my_value"

# Изменяем рабочую директорию
WorkingDirectory=/var/www/myapp
