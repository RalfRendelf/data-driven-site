<p><strong>Цель:</strong> Создать новую категорию для раздела сайта.</p>

<p>Структура объекта:</p>
<pre><code>{
  "title": "🧪 Новая категория",
  "type": "имя_в_путях",
  "columns": ["Название", "Описание", "Тип", ...]
}
</code></pre>

<p>Пояснение:</p>
<ul>
  <li><strong>title</strong> — заголовок категории (можно с иконкой)</li>
  <li><strong>type</strong> — ключ, по которому находятся index.json и .txt файлы</li>
  <li><strong>columns[]</strong> — список полей в таблице, длина может быть любой</li>
</ul>

<p>При генерации:</p>
<ul>
  <li>Учитывай, что портал подгружает данные из <code>data/{type}/index.json</code></li>
  <li>Каждая колонка будет отображена в таблице</li>
  <li>Система поддерживает любое количество колонок</li>
</ul>
