> Сгенерируй HTML-файл в формате .txt для системы динамической загрузки паттернов. Название файла: ТИП_ИМЯ.txt, где ТИП — это паттерны или архитектуры, ИМЯ — название паттерна (на латинице, без пробелов, например: singleton, mvc). > > Формат содержимого: > 1. Описание (<p><strong>Назначение:</strong> ...</p>) > 2. Когда использовать (<ul>) > 3. Преимущества (<ul>) > 4. Недостатки (<ul>) > 5. Пример кода (<pre><code>...</code></pre>) > > Используй HTML-теги (<p>, <ul>, <pre>, <code>) и эскейп-теги (&lt;, &gt;) там, где нужно. Пример должен быть на Java или C++. > Код должен быть минимально достаточным, читаемым и фокусироваться на сути паттерна. > Весь текст — на русском. > > Выводи только HTML-контент внутри .txt-файла, без пояснений и заголовков