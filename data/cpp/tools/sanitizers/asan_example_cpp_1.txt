# Компиляция с ASan (Clang или GCC)
clang++ -fsanitize=address -g -std=c++17 asan_bug.cpp -o asan_program
# или
g++ -fsanitize=address -g -std=c++17 asan_bug.cpp -o asan_program

# Пример кода с ошибкой: выход за границы массива
#include <iostream>

int main() {
    int* arr = new int[5];
    arr[5] = 42; // Ошибка: индекс 5 вне диапазона [0..4]
    delete[] arr;
    return 0;
}

# При запуске:
# ==12345==ERROR: AddressSanitizer: heap-buffer-overflow
# Укажет строку, стек вызовов и адрес ошибки.
