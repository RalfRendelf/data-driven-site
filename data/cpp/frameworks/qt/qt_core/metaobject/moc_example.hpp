#include <QObject>

class MyClass : public QObject {
    // Этот макрос обязателен! Он запускает MOC (Meta-Object Compiler),
    // который генерирует код для работы сигналов, слотов и интроспекции.
    Q_OBJECT

public:
    // Стандартный конструктор. Передача parent помогает Qt 
    // автоматически удалять объект, когда удаляется его "родитель".
    explicit MyClass(QObject *parent = nullptr);

public slots:
    // Метод, который можно вызвать не только напрямую, но и через
    // систему сигналов/слотов или QMetaObject::invokeMethod.
    void mySlot();
};

/* 
  ЧТО ЭТО И ЗАЧЕМ НУЖНО:

  1. Наследование от QObject:
     Дает классу "суперспособности" Qt: работу с потоками (moveToThread), 
     таймеры, иерархию объектов и, конечно, событийную модель.

  2. Макрос Q_OBJECT:
     Без него вы не сможете использовать сигналы и слоты. Именно он 
     создает ту самую структуру метаданных, которую мы читали через 
     `obj.metaObject()`.

  3. Секция public slots:
     Это обычный метод C++, но помеченный для метаобъектной системы. 
     Это делает его видимым для других объектов, скриптов (QML) 
     и позволяет вызывать его по имени-строке.

  4. Explicit конструктор:
     Хорошая практика C++, предотвращающая случайные неявные 
     преобразования типов (например, чтобы вы случайно не передали 
     число там, где ожидается указатель на объект).
*/
