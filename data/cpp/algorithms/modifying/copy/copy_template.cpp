// 1. Подключите заголовок <algorithm>
#include <algorithm>

// 2. Убедитесь, что целевой диапазон имеет достаточно места
// (или используйте вставляющие итераторы, например, std::back_inserter)

// 3. Вызовите std::copy
auto result_iterator = std::copy(
    source_first,  // начало исходного диапазона
    source_last,   // конец исходного диапазона
    dest_begin     // начало целевого диапазона
);

// 4. result_iterator указывает на элемент после последнего скопированного
